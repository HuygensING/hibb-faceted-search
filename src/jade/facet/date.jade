header
	h3(data-name!="<%= name %>") <%= title %>

.body
	label From:
	select
		| <% _.each(options, function(option) { %>
		option <%= option %>
		| <% }); %>
	label To:
	select
		| <% _.each(options.reverse(), function(option) { %>
		option <%= option %>
		| <% }); %>